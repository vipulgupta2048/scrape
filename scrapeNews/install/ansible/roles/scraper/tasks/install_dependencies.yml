---

- name: update cache
  apt:
    update_cache: yes

- name: Install/Update Dependencies
  apt:
    name: "{{ item }}"
    state: latest
  with_items:
    - "gcc"
    - "git"
    - "tmux"
    - "python3"
    - "python3-dev"
    - "virtualenv"
    - "nginx"
  notify:
    - restart postgresql
    - restart nginx
  